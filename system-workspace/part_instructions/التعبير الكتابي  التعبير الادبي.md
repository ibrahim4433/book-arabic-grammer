<!-- DYNAMIC INSTRUCTION FOR PART: التعبير الكتابي / التعبير الأدبي -->

CRITICAL STRUCTURAL BLUEPRINT FOR "WRITING EXPRESSION / LITERARY ESSAY":
You MUST map this part exactly following this visual hierarchy to create the essay mind-map/outline:

1. THE MAIN CONTAINER:
You MUST use `TEMPLATE_C_BLOCK.html` (`.content-block`) to house the entire outline:
- **Header**: Use `<div class="block-header">` and place the title inside (e.g., <span>التعبير الكتابي - التعبير الأدبي</span>).
- **Sub-Title**: Inside the `.block-body`, begin with a bold centered paragraph for the essay topic: `<p class="mt-0 text-accent text-xs font-bold text-center mb-0">مخطط موضوع...</p>`.

2. THE NESTED OUTLINE LIST:
Beneath the sub-title, you MUST use nested `TEMPLATE_C_LIST.html` (`<ul class="structured-list">`) to build the outline hierarchy. 
You MUST adhere strictly to the following nesting levels, markers, and color classes:

- **Level 1 (Main Sections - e.g., مقدمة مناسبة, الأدب القومي):**
  Use bullet marker: `<span class="marker">•</span>`
  Use text class: `<span class="text-primary font-bold">`

- **Level 2 (Main Ideas/Themes - e.g., الدعوة إلى التنبه):**
  Use dash marker: `<span class="marker">-</span>`
  Use text class: `<span class="highlight-blue font-bold">`

- **Level 3 (Sub-Ideas or Poet Names - e.g., إبراهيم اليازجي):**
  Use chevron marker: `<span class="marker">»</span>`
  Use text class: `<span class="text-accent font-bold">`

- **Level 4 (Specific Verses / Details):**
  Use square marker: `<span class="marker">▪</span>`
  Use standard text (no special color class needed, just default or text-accent).

*Important Nesting Rule:* When opening a nested `<ul>` inside an `<li>`, ensure you remove top margins to keep it compact: `<ul class="structured-list mt-0">`.

3. GENERAL RULE:
- The root `.content-block` MUST have a unique ID (`id="bXXXXX"`).
- Maintain tight padding (`p-0` or `p-1mm`) to prevent A4 page overflow, as nested lists can consume vertical space rapidly.
